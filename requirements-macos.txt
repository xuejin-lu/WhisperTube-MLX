# Local-only v0.2 inference dependencies for macOS Apple Silicon.
# CI intentionally does not install this file or download model weights.
# Install the system decoder separately with: brew install ffmpeg
mlx-whisper==0.4.3
OpenCC==1.4.2
